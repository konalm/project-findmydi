import gmap from './gmap';
import address from './address';

var location = {
  init: function(coverages) {
    gmap(coverages)
  }
};


const location2 = (coverages) => {
  gmap(coverages)
}

export default location;
